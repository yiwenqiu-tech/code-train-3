首先，变长数组的基本功能是增删改查。
其与定长数组的区别是：
定长数组的长度是固定的，在定义时就分配好，使用时不可以越界；
而变长数组则可以根据使用的长度动态调整；

核心理念：
1.当空间不够时，重新申请一块两倍的空间，然后把原来的空间拷贝至新空间上；
2.当空间利用率少于25%时，缩容；

slice实现
https://books.studygolang.com/GoExpertProgramming/chapter01/1.2-slice.html
